{
  "name": "react-node-procfile",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "deploy": "npm run build && git add . && git commit -m 'Update' && git push live master",
    "build": "cd client && npm run build && cd ..",
    "start": "nf start -p 3000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.15.2",
    "body-parser": "^1.15.2",
    "eslint-config-fortech": "^1.0.0",
    "express": "^4.14.0",
    "foreman": "^2.0.0",
    "jsonwebtoken": "^7.1.9",
    "nedb": "1.8.0 "
  }
}
